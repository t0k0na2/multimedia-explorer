<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="main.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:main"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="main">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Control Panel -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Padding="16" Spacing="16">
            <Button Content="ディレクトリを選択" Click="SelectDirectoryButton_Click" />
            <TextBlock x:Name="SelectedDirectoryTextBlock" VerticalAlignment="Center" Text="ディレクトリが選択されていません。" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
        </StackPanel>

        <!-- GridView for Files and Folders -->
        <GridView Grid.Row="1" x:Name="FileSystemGridView" Padding="16" SelectionMode="Single">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local:FileSystemItem">
                    <StackPanel Width="120" Height="140" Padding="8" Spacing="8" Background="Transparent" CornerRadius="4">
                        <Grid Height="48" Margin="0,16,0,0">
                            <FontIcon Glyph="{x:Bind Icon}" FontSize="48" Visibility="{x:Bind FontIconVisibility}" HorizontalAlignment="Center"/>
                            <Image Source="{x:Bind ImageSource}" Stretch="Uniform" Visibility="{x:Bind ImageIconVisibility}" HorizontalAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="{x:Bind Name}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" MaxLines="2" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Window>
